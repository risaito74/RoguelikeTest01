# 検証手順

## リファクタリング検証
- [ ] プレイヤーの移動、壁判定、マップ描画が正常か確認

## NPC「エミ」追加検証
- [ ] **表示確認**
    - ゲーム開始時、座標(3,3)に 'E' が表示されているか。
- [ ] **会話確認**
    - プレイヤーを操作して 'E' に隣接し、その方向へ移動しようとする。
    - 移動がブロックされるか（重ならない）。
    - メッセージエリアに "Watashi ha Emi dayo!" と表示されるか。
- [ ] **壁配置エラー確認（開発者向け）**
    - `GameManager.cs` の `InitNPCs` 内で座標を壁の位置（例: (0,0)）に変更して実行。
    - Consoleに "NPC 'Emi' cannot be placed..." というエラーが出るか確認。
